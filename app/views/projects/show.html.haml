%h1= @project.title
%p= @project.description
%h2 Tasks
%hr
%h3= link_to('New Task', new_project_task_path(@project))
%ul
  - @project.tasks.each do |task|
    %li= link_to(task.title, project_task_path(@project, task))
